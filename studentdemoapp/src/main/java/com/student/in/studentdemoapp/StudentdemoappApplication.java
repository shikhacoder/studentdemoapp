package com.student.in.studentdemoapp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class StudentdemoappApplication {

	public static void main(String[] args) {
	SpringApplication.run(StudentdemoappApplication.class,args);

		/*
		 * StudentService studentService = applicationContext.getBean("studentService",
		 * StudentService.class);
		 * 
		 * Student student = new Student();
		 * 
		 * student.setStuFname("Shikha"); student.setStuLname("Dawre");
		 * student.setAddress("Indore"); student.setMobileNo(789456123); Department
		 * department =new Department(name "IT");
		 * 
		 * studentService.createStudent(student);
		 */	
		/*
		 * DepartmentService departmentService =
		 * applicationContext.getBean("departmentService", DepartmentService.class);
		 * Department department=new Department(); department.setDeptName("Accounts");
		 * department.setCourse("MBA"); List<Student> student=new ArrayList<>();
		 * 
		 */
		
		
		
		
	}

}
